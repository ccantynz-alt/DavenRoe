
### 3. `.github/copilot-instructions.md`
Copy the content from `ai-ledger/CLAUDE.md` — it already has the stack, locked decisions, key directories, build commands, and jurisdictions. That's exactly what Copilot needs.

---

If you add this repo to the session's authorized repos, I can do it directly. Otherwise just paste these in.
