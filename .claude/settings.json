{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/Ledger.Ai-/frontend && npm run build --silent > /tmp/astra-build.log 2>&1 && echo '{\"systemMessage\": \"Build passed\"}' || echo '{\"systemMessage\": \"BUILD FAILED — check /tmp/astra-build.log\"}'",
            "timeout": 120,
            "statusMessage": "Running build check..."
          }
        ]
      }
    ]
  }
}
